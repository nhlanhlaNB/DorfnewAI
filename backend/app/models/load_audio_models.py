# Stub until models are integrated with inference
def generate_audio(prompt: str, model: str = "suno"):
    print(f"[AUDIO GEN] Model: {model}, Prompt: {prompt}")
    # TODO: integrate Suno / Bark audio generation
    # Save file or return bytes as needed
